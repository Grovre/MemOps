namespace MemOps.Aob.Strategies;

public class VectorScan : IScanStrategy
{
    public IntPtr Scan(ReadOnlySpan<byte> pattern, ReadOnlySpan<byte> mask, ReadOnlySpan<byte> data)
    {
        throw new NotImplementedException();
    }
}